import test from 'node:test';import assert from 'node:assert/strict';import {makeEpochToken,isValidEpochToken,normalizeEncounter,upsertEncounter} from '../../../apps/mobile/src/nearbyLogic.js';
test('tokens are compact, stable per epoch input and device salt',()=>{const token=makeEpochToken(1_700_000_000_000,()=>0.5);assert.equal(token.length,4);assert.equal(isValidEpochToken(token),true);assert.equal(isValidEpochToken('123456789'),false)});
test('encounters reject malformed tokens and normalize proximity',()=>{assert.equal(normalizeEncounter({epochToken:'123456789'}),null);assert.deepEqual(normalizeEncounter({epochToken:'c123',rssiBucket:'other',observedAt:42}),{id:'c123',epochToken:'c123',rssiBucket:'uncertain',observedAt:42})});
test('encounters de-duplicate newest observation by token',()=>{const first=upsertEncounter([],{epochToken:'c123',rssiBucket:'uncertain',observedAt:1});const second=upsertEncounter(first,{epochToken:'c123',rssiBucket:'likely-near',observedAt:2});assert.equal(second.length,1);assert.equal(second[0].rssiBucket,'likely-near');assert.equal(second[0].observedAt,2)});
